---
name: doc-impl-parity-audit
description: 审「文档声明」与「代码实现」是否一致 —— 抓 CLI 参数/子命令/字段名/枚举值/索引锚点的脱节，尤其是"照文档执行 → 工具写入 → 同一工具自己判 FAIL"这类自相矛盾。适用于：改完 CLI/MCP/skill 工具或 SKILL.md 后要验收、文档里写了新命令或新字段、被要求"确认文档跟实现对得上"、独立审核只抓到"描述与实现不符"类问题。产出一份带双方原文的逐项差异清单，而不是一句"已核对一致"。
agent_created: true
version: 1.0.0
---

# 文档 ↔ 实现一致性审计

改完一个带文档的 CLI / MCP 工具 / skill，检查「文档说它有什么」与「代码真有什么」是否一致。

## 何时用

- 刚给工具加了子命令 / 参数 / 配置字段，同时改了文档
- 被要求「确认文档跟实现对得上」
- 独立审核抓出的问题**全是**「文档写了但代码没有 / 名字不一致」这一类
- 交付验收时想给「文档已同步」这句话配上证据

## 一、先接受三个反直觉事实

### ① 作者自查对这类问题**系统性失效**

不要把「我记得同步改了」当成检查。实测数据：一次改造经两轮独立审核，共抓出 **7 项**问题，
**7 项全部**属于「文档声明 ↔ 实现」脱节 —— 没有一项是逻辑 bug、性能问题或边界条件。

原因很朴素：改文档和改代码是两次独立操作，**注意力在中途切换过一次**，
而人对「我打算做的」和「我实际做的」记忆会合并成同一件事。
（这条与 IDENTITY 里那条自我弱点一致：**作者自查 ≠ 审核**。）

### ② 常规测试**抓不到**这类问题

- `py_compile` / `import` / 单元测试全过 —— 它们验证**实现**，不验证**文档**。
- 文档里写了一条不存在的命令，测试不会因为它失败。
- 实测：技能自检 **19/19 全绿**的同时，文档里存在 **7 项**脱节。

⇒ 必须有一道**专门面向文档**的检查，否则这块永远是盲区。

### ③ 最锋利的一类：**工具自相矛盾**

不是"文档写漏了"，而是：**照文档执行 → 工具写入数据 → 同一个工具自己的校验判它 FAIL。**

实测案例（该案例是新建本 skill 的直接原因）：

- 文档里 `decide` 子命令的示例**没写 `--choice`**；
- 而校验逻辑里 `DECISION_KEYS = ("决策点", "能力类", "依据", "选择")`，强制「选择」非空；
- 用户完全照文档操作 → 写入一条记录 → 下一次 `plan` 对账时 **FAIL**。

这类问题**只有真的去执行一遍文档里的命令**才会暴露，光读文档读不出来。

## 二、四步法

### 第一步（性价比最高）：把文档里的命令**逐条实际执行一遍**

```bash
# 用文档原文里的【最简示例】，不要用作者脑子里的完整命令
<按文档原文抄写的命令>
echo "exit=$?"
```

两条纪律：

- **抄文档原文，不要补全。** 用户会照抄的正是这一段；作者自己会下意识补上缺的参数，于是测不出问题。
- **退出码非 0 / 报缺少参数 / 报未知子命令 ⇒ 立刻命中。** 本次靠这一步一次抓下 2 项（一项是不存在的子命令参数，一项是缺必填参数）。

> 这一步通常是**发现问题的最高密度动作**。先做它，再谈别的。

### 第二步：**双向**枚举（只做一个方向必漏）

| 方向 | 做法 | 抓到的是 |
|---|---|---|
| **文档 → 实现** | 抽出文档里所有 `--flag` / 子命令 / 字段名 / 枚举值，逐个确认实现认识它 | 幽灵命令、幽灵参数 |
| **实现 → 文档** | 枚举 `add_argument()` 的 flag、常量元组里的取值、模板的 key，逐个确认文档有写 | 文档漏写、名字不一致 |

实测：流程方向抓到的两项各属不同方向 ——
「校验文案声称 5 个字段、实际只强制 3 个」属第一方向；
「手册里把字段名写成「候选」、实现里叫「能力类」」属第二方向。
**只做任一方向都会漏掉另一类。**

可直接用配套脚本：

```bash
python scripts/doc_impl_parity.py --doc SKILL.md references/*.md --impl scripts/tool.py
```

### 第三步：把「单一事实源」立起来

**校验失败文案里的字段列表，绝不要手写。**

```python
# 错：文案里手写字段名 —— 改实现时必然忘记改它，于是文案说谎
fail("决策记录格式", "每项必含 决策点/能力类/依据/选择/时间")

# 对：文案由常量生成 —— 实现是唯一事实源
DECISION_KEYS = ("决策点", "能力类", "依据", "选择")
fail("决策记录格式", f"应为列表（每项必含 {'/'.join(DECISION_KEYS)}；时间由工具自动填）")
```

同理适用于：枚举值清单、默认值、参数个数、上限值。**任何在文档和代码里各写一遍的东西，都会漂移。**

### 第四步：**阴性对照** —— 让它证明自己抓得到

检查器全绿**没有意义**，除非你能证明它**在错误存在时会红**。

- 造一个「照文档手写」的 fixture（文档写错时，这个 fixture 会被自己的门禁判 FAIL ⇒ 一举暴露矛盾）
- 造一个「字段名写错 / 枚举值越界 / 必填项为空」的 fixture，确认逐项 FAIL
- 造一个「缺参数」的调用，确认退出码为 2（而不是静默成功）

## 三、四个必踩的坑

### ① 幽灵字段：模板加了字段，校验**零引用**

看起来"加了机制"，实际**没有任何代码读它** —— 比没有更糟，因为它制造"已被约束"的错觉。

**规则：新增一个可配置字段，必须同时说清「谁校验它」。说不出来就删掉。**

### ② 文档字段名与常量不一致

`「候选」` vs `「能力类」` —— 人读着觉得是一回事，机器判 FAIL。
**用程序化比对兜底**（而不是肉眼扫）：

```python
doc_fields  = extract_fields_from_markdown(path)   # 从表格/反引号抽
impl_fields = set(DECISION_KEYS)
print("一致：", set(doc_fields) == impl_fields)
```

### ③ 索引不成立：正文说「详见 X.md」，而 X.md 里没有

新增内容时最容易漏。**每个「详见/参见」都要去目标文件确认那段真的存在**（顺带确认章节号、命令名、变更日志版本号都更新了）。

> 实测：正文写「完整参数见 ops.md」，而 ops.md 里既没有该子命令、变更日志也停在上一版。

### ④ 回归证据的文件清单**不要手写**

返修阶段新增/修改了一个文件，手写的清单不会自动包含它 ⇒ 证据漏收改动面。
**清单从 `git status --short` 或 `git diff --name-only <base>..<head>` 生成。**

## 四、报告格式

逐项列出，**每项都要贴双方原文**（只给结论等于没给证据）：

| 文档位置 | 文档原文 | 实现原文 | 差异 | 级别 |
|---|---|---|---|---|
| `SKILL.md` 第 N 行 | `checks.py mark --revise` | 无该参数（实为独立子命令 `revise`） | 命令不存在 | 高 |
| `plan-template.yaml` | `决策依据: []` | 全文无引用 | 幽灵字段 | 高 |

级别建议：

- **高**：照文档执行会**失败**，或会**写入非法数据**（自相矛盾类）
- **中**：文档漏写/多写，不直接致错但误导
- **低**：措辞、锚点行号、示例过时

## 五、诚实边界（写进结论）

- **自检通过 ≠ 无脱节**：19/19 全绿时仍有 7 项脱节，两者互不覆盖。
- **只覆盖可机械抽取的部分**：参数名、字段名、枚举值、命令名。语义级描述（"尽力""建议""默认"）仍靠人读。
- **无法区分「调用指引」与「历史记述」**：文档里写「原先把 `X --version` 标为首选，该命令**恒失败**」是在**讲述一个错误**，不是让你去用它 —— 工具会把它报成幽灵参数。这类只能靠人读上下文判定。
- **受控场景准，真实场景噪**（实测，见第六节表格）：人为埋错的 fixture 上精确率 100%；真实项目 10 万字符文档上 6 项命中**全是假阳性**。**别把报告当问题清单。**
- **抽取不到东西时退出码 2（UNKNOWN），不许报绿** —— 否则"没抽到"会被误当成"没问题"。
- 改了工具文档后**要重跑**；一次性结论会随下次改动过期。

## 六、配套脚本

`scripts/doc_impl_parity.py` —— 零依赖，双向差集：

```bash
# 两边的文件都要传【全】—— 只传一部分会制造整片假差集
python scripts/doc_impl_parity.py \
    --doc SKILL.md references/*.md \
    --impl scripts/*.py
```

| 退出码 | 含义 |
|---|---|
| 0 | 文档未承诺任何实现里没有的东西（幽灵命令/参数/字段 = 0） |
| 1 | **文档承诺了实现里没有的东西** —— 唯一"照文档执行会出事"的方向 |
| 2 | 不可判定（某侧抽取为空 —— 路径写错或格式不认识） |

三个桶：

| 桶 | 抽取来源 |
|---|---|
| `flag` | `--flag`，来自反引号 token + 围栏/缩进代码块 |
| `subcmd` | `脚本.py 子命令` |
| `field` | 字段名/枚举值；只把**中文短词**计入判定 |

**退出码只由「文档承诺了实现里没有的东西」驱动。** 反方向（实现有、文档未写）一律只作参考：文档本来就不必穷举所有参数与内部常量（API 返回字段、内部函数名都不是文档该管的事），实测该项在真实项目里稳定刷出上百条，参与判定会让工具长期假红而失去意义。

### 实测精度（先读这个，再决定信多少）

| 场景 | 高置信命中 | 其中真问题 | 说明 |
|---|---|---|---|
| 受控 fixture（人为埋 3 处错） | 3 | **3** | 漏检 0、误报 0 |
| 真实项目（4 份文档 ≈ 10 万字符 / 5 个脚本） | 6 | **0** | 全部假阳性 |

⇒ **它的价值是「把候选从 10 万字符缩到个位数」，不是「直接给出问题清单」。**
缩窄之后必须人工逐项判。真实项目那 6 项的假阳性归因：

| 归因 | 例子 | 能否机械化 |
|---|---|---|
| 文档在**描述历史 bug**（"曾写 X，该参数不存在"） | `--revise` | 不能 —— 需语义判断 |
| 抽取口径局限（子命令用 `sys.argv` 手动分派，非 `add_parser`） | `fetch` | 能，但成本高 |
| 文档里的**普通叙述词**，不是声明 | `通过`、`体验报告` | 不能 |

### 已做的降噪（这些改动本身就是方法的一部分）

都是踩出来的，供后续维护者理解"为什么这么写"：

- 文档侧**不抽表格裸单元格** —— 表格里的"行为""质量""通过"不是字段声明，抽进来一次就多出上百条噪声，把真线索淹掉（这是 131 → 42 的主因）
- 剔除**文件/路径形态** token（`plan.yaml`、`a/b`）出字段桶
- 屏蔽 `argparse` **无条件自建**的 `--help`/`-h`（从 argparse 自身取名单，不硬编码）
- 实现侧补抽**中文字符串字面量** —— 真字段常写作 `meta.get("熔断状态")` 而非常量元组，只抽常量元组会把它误报成幽灵字段
- 文档侧补抽**代码块**（围栏 + 缩进）—— 补之前会**整个漏检**"写在缩进块里的幽灵参数"，那是假阴性，比假阳性更危险
- 反方向判据用**文档全文 substring**，而非"文档抽取结果" —— 文档不会把每个名字都加反引号，用抽取集比会刷出 24/24 的假阳性
- 报告头给出**覆盖完整性**提示：文档引用了但没传进 `--impl` 的脚本，其参数会被误报成幽灵参数 —— 这是**覆盖缺口**，不是真差集

脚本只负责**把两侧声明摊开对比**，不下"是否有问题"的结论 —— 结论由人给。

### 自检证据（可复核）

- `tests/自检证据.txt` —— 四组测试的**原始输出与退出码**：T1 编译 / T2 不可判定（退出码 2）/ T3 真实项目 / T4 阴性对照
- `tests/fixture_doc.md` + `tests/fixture_impl.py` —— 阴性对照 fixture（埋在**缩进代码块**里的幽灵参数 + 幽灵字段），可随时重跑，用来证明工具"抓得到东西"
- `tests/t3_report.json` —— T3 的结构化结果

重跑全部四组：

```bash
PY=~/.workbuddy/binaries/python/envs/ai-workflow/Scripts/python.exe
"$PY" -m py_compile scripts/doc_impl_parity.py
"$PY" scripts/doc_impl_parity.py --doc "no_such_*.md" --impl scripts/doc_impl_parity.py; echo "T2 exit=$? (期望 2)"
"$PY" scripts/doc_impl_parity.py --doc tests/fixture_doc.md --impl tests/fixture_impl.py; echo "T4 exit=$? (期望 1)"
```
